import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'locations_csv',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
